// Place your settings in this file to overwrite default and user settings.
{
    "files.exclude": {
        "**/*.js.map": {
            "when": "$(basename)"
        },
        "**/*.js": {
            "when": "$(basename).ts"
        },
        "app/common/*.js":true,
        "app/common/*.js.map":true
    }
}